--- a/src/pd_pki_workflow/gui.py
+++ b/src/pd_pki_workflow/gui.py
@@ -652,6 +652,18 @@
       }
 
       function workflowConfig() {
+        if (configLocked) {
+          return {
+            workflowRoot: lockedWorkflowConfig.workflowRoot || "",
+            profileDir: lockedWorkflowConfig.profileDir || "",
+            tokenDir: lockedWorkflowConfig.tokenDir || "",
+            workspaceDir: lockedWorkflowConfig.workspaceDir || "",
+            bundleDir: lockedWorkflowConfig.bundleDir || "",
+            repositoryRoot: lockedWorkflowConfig.repositoryRoot || "",
+            rootName: lockedWorkflowConfig.rootName || "",
+          };
+        }
+
         return {
           workflowRoot: document.getElementById("workflow-root").value.trim(),
           profileDir: document.getElementById("profile-dir").value.trim(),
